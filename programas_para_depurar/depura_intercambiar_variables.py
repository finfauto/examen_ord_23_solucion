from errores import intercambiar_variables

if __name__ == "__main__":
    a =  # <- Rellena este dato
    b =  # <- Rellena este dato
    nueva_a, nueva_b = intercambiar_variables(a, b)

    print(f"a vale {a} y debe ser igual que nueva_b, que vale {nueva_b}")
    print(f"b vale {b} y debe ser igual que nueva_a, que vale {nueva_a}")
